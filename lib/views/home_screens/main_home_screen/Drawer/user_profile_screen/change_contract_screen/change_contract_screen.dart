import 'package:flutter/material.dart';

class ChangeContractScreen extends StatelessWidget {
  const ChangeContractScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Change Contract'),
      ),
    );
  }
}
